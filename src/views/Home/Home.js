import styled from 'styled-components';
import { spacing } from '../../utils/sizes';

const Container = styled.div`
    padding: ${spacing[48]};
`;

const Home = () => {
    return (
        <Container>
            Home
        </Container>
    )
}

export default Home
